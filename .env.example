# Environment Variables for JSON Database

# Enable JSON database mode (true = use JSON, false = use real API)
REACT_APP_USE_JSON_DB=true

# API Configuration (when not using JSON DB)
# REACT_APP_API_URL=http://localhost:3000/api
# REACT_APP_API_TIMEOUT=10000

# Add other environment variables as needed
